  <div class="modal">
    <div class="modal-header">
      <button type="button" class="close" aria-hidden="true">×</button>
      <h3>{{heading}}</h3>
    </div>
    <div class="modal-body">
      {{#if embedUrl}}
        {{> VideoEmbed}}
      {{/if}}
    </div>
  </div>
