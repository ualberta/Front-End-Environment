<div class="{{type}} carousel-container">
    <div class="slides">
      {{#each features}}
        {{> Feature}}
      {{/each}}
    </div>
    <ul class="toggles">
      {{#each_with_index features}}
        <li>{{index}}</li>
      {{/each_with_index}}
    </ul>
</div>
